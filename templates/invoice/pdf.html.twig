<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Fatura {{ invoice.invoiceNumber }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        @page {
            margin: 25mm 20mm 30mm 20mm;
        }
        
        .invoice-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 9px;
            color: #666;
            padding: 10mm 20mm;
            border-top: 1px solid #e0e0e0;
            background: white;
        }
        
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 12px;
            color: #333;
            line-height: 1.5;
            background: #fff;
        }
        
        .invoice-container {
            max-width: 100%;
            margin: 0 auto;
            background: white;
            padding: 10mm 5mm;
        }
        
        /* Invoice Header */
        .invoice-header {
            margin-bottom: 25px;
        }
        
        .invoice-header-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .invoice-header-table td {
            vertical-align: middle;
        }
        
        .invoice-header h1 {
            font-size: 18px;
            color: #333;
            margin: 0;
        }
        
        .invoice-header-info {
            text-align: right;
            font-size: 11px;
        }
        
        .invoice-date-large {
            font-size: 11px;
            font-weight: 600;
        }
        
        .invoice-date-normal {
            font-size: 11px;
            font-weight: 600;
        }
        
        .invoice-header-info > div {
            margin-bottom: 3px;
            line-height: 1.8;
        }
        
        .invoice-header-info strong {
            font-weight: 600;
            color: #666;
            margin-right: 5px;
        }
        
        .invoice-header-info span {
            color: #333;
        }
        
        .status-badge {
            display: inline;
            font-size: 11px;
        }
        
        /* Businesses Section */
        .businesses-section {
            width: 100%;
            margin-bottom: 25px;
        }
        
        .businesses-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 15px 0;
        }
        
        .business-card {
            width: 50%;
            background: #fcfcfc;
            border-radius: 8px;
            padding: 12px;
            vertical-align: top;
        }
        
        .business-card h3 {
            font-size: 14px;
            margin: 0 0 8px 0;
            color: #333;
            border-bottom: 1px solid #e0e0e0;
            padding-bottom: 5px;
        }
        
        .business-logo {
            max-width: 100px;
            max-height: 60px;
            margin-bottom: 8px;
            display: block;
        }
        
        .business-details {
            font-size: 9px;
        }
        
        .business-details p {
            margin: 4px 0;
            color: #666;
            line-height: 1.3;
        }
        
        .business-details strong {
            color: #333;
            font-weight: 600;
            margin-right: 5px;
        }
        
        /* Items Section */
        .items-section {
            margin-bottom: 25px;
        }
        
        .items-section h2 {
            font-size: 18px;
            color: #333;
            margin-bottom: 15px;
            border-bottom: 2px solid #e0e0e0;
            padding-bottom: 8px;
        }
        
        .items-table-container {
            margin-top: 15px;
        }
        
        .items-table {
            width: 100%;
            border-collapse: collapse;
            background: white;
        }
        
        .items-table thead {
            background: #f5f5f5;
        }
        
        .items-table th {
            padding: 0;
            text-align: center !important;
            font-weight: 600;
            color: #333;
            border-bottom: 2px solid #e0e0e0;
            font-size: 11px;
        }
        
        .items-table thead .text-right,
        .items-table thead .text-center {
            text-align: center !important;
        }
        
        .items-table tbody tr {
            border-bottom: 1px solid #e0e0e0;
            padding: 0;
            margin: 0;
        }
        
        .items-table tbody tr:last-child {
            border-bottom: none;
        }
        
        .items-table td {
            padding: 0;
            margin: 0;
            color: #666;
            font-size: 11px;
            vertical-align: middle;
            white-space: nowrap;
        }
        
        .items-table td.description {
            white-space: normal;
            word-wrap: break-word;
        }
        
        .items-table .text-right {
            text-align: right;
        }
        
        .items-table .text-center {
            text-align: center;
        }
        
        .items-table tbody tr td strong {
            color: #333;
            font-weight: 600;
        }
        
        .tax-exempt {
            color: #9e9e9e;
            font-style: italic;
        }
        
        /* Totals Section */
        .totals-section {
            margin-top: 25px;
            margin-bottom: 25px;
        }
        
        .totals-wrapper {
            width: 100%;
            border-collapse: separate;
            border-spacing: 20px 0;
        }
        
        .totals-wrapper td {
            vertical-align: middle;
        }
        
        .qr-wrapper {
            width: 100px;
        }
        
        .totals-card {
            background: #fcfcfc;
            border-radius: 8px;
            padding: 15px;
            min-width: 300px;
            text-align: right;
        }
        
        .totals-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .totals-table tr {
            border-bottom: 1px solid #e0e0e0;
        }
        
        .totals-table tr:last-child {
            border-bottom: none;
        }
        
        .totals-table td {
            padding: 6px 0;
        }
        
        .total-label {
            font-weight: 600;
            color: #333;
            font-size: 10px;
            text-align: left;
        }
        
        .total-value {
            font-weight: 600;
            color: #333;
            font-size: 10px;
            text-align: right;
        }
        
        .total-tax {
            border-top: 1px solid #e0e0e0;
            margin-top: 5px;
            padding-top: 8px;
        }
        
        .tax-subtable {
            width: 100%;
            margin-top: 8px;
            border-collapse: collapse;
        }
        
        .tax-subtable tr {
            border-bottom: none;
        }
        
        .tax-subtable td {
            padding: 4px 0;
            font-size: 10px;
        }
        
        .tax-subtable .tax-rate-label {
            text-align: left;
            color: #666;
            padding-left: 15px;
        }
        
        .tax-subtable .tax-rate-value {
            text-align: right;
            color: #333;
        }
        
        .total-final {
            border-top: 2px solid #333;
            margin-top: 8px;
            padding-top: 8px;
        }
        
        .total-final .total-label,
        .total-final .total-value {
            font-size: 12px;
            font-weight: 700;
        }
        
        /* QR Code Section */
        .qr-section {
            text-align: center;
        }
        
        .qr-section img {
            width: 70px;
            height: 70px;
            display: block;
            margin: 0 auto 5px;
            border: 1px solid #ddd;
        }
        
        .qr-section-label {
            font-size: 9px;
            color: #666;
            font-weight: bold;
        }
        
        /* Bank Account Section */
        .bank-accounts-wrapper {
            width: 250px;
            vertical-align: top;
        }
        
        .bank-accounts-card {
            background: #fcfcfc;
            border-radius: 8px;
            padding: 12px;
        }
        
        .bank-accounts-card h4 {
            font-size: 11px;
            margin: 0 0 8px 0;
            color: #333;
            font-weight: 700;
            border-bottom: 1px solid #e0e0e0;
            padding-bottom: 5px;
        }
        
        .bank-account-item {
            margin-bottom: 10px;
            font-size: 9px;
        }
        
        .bank-account-item:last-child {
            margin-bottom: 0;
        }
        
        .bank-account-item p {
            margin: 3px 0;
            color: #666;
            line-height: 1.3;
        }
        
        .bank-account-item strong {
            color: #333;
            font-weight: 600;
            margin-right: 5px;
        }
        
        /* Page breaks */
        .items-section {
            page-break-inside: avoid;
        }
        
        .totals-section {
            page-break-inside: avoid;
        }
        
        .businesses-section {
            page-break-inside: avoid;
        }
        
        /* Signature Section */
        .signature-section {
            position: fixed;
            bottom: 30mm;
            left: 20mm;
            right: 20mm;
            margin-top: 40px;
            padding-bottom: 10px;
        }
        
        .signature-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .signature-cell {
            width: 50%;
            vertical-align: top;
            padding: 0 20px;
        }
        
        .signature-line {
            border-top: 1px solid #333;
            margin-top: 50px;
            padding-top: 5px;
            font-size: 10px;
            color: #333;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <!-- Invoice Header -->
        <div class="invoice-header">
            <table class="invoice-header-table">
                <tr>
                    <td>
                        <div style="display: flex; align-items: center; gap: 15px; flex-wrap: nowrap;">
                            {% if issuerLogo %}
                            <img src="data:{{ issuerLogo.mime }};base64,{{ issuerLogo.data }}" alt="Logo" style="max-width: 120px; max-height: 80px; object-fit: contain; flex-shrink: 0;" />
                            {% endif %}
                            <h1 style="margin: 0; white-space: nowrap;">Fatura {{ invoice.invoiceNumber }}</h1>
                        </div>
                    </td>
                    <td class="invoice-header-info">
                        <div>
                            <strong>Data e Faturës:</strong>
                            <span class="invoice-date-large">{{ invoice.invoiceDate ? invoice.invoiceDate.format('d.m.Y') : '' }}</span>
                        </div>
                        <div>
                            <strong>Data e Maturimit:</strong>
                            <span class="invoice-date-normal">{{ invoice.dueDate ? invoice.dueDate.format('d.m.Y') : '' }}</span>
                        </div>
                    </td>
                    {% if qrCodeBase64 %}
                    <td style="text-align: right; padding-left: 20px;">
                        <div class="qr-section">
                            <img src="data:image/png;base64,{{ qrCodeBase64 }}" alt="QR Code" />
                            <div class="qr-section-label">Skano për të parë online</div>
                        </div>
                    </td>
                    {% endif %}
                </tr>
            </table>
        </div>
        
        <!-- Issuer and Receiver -->
        <div class="businesses-section">
            <table class="businesses-table">
                <tr>
                    <td class="business-card">
                        <h3>Fatura Nga: {{ invoice.issuer.businessName }}</h3>
                        <div class="business-details">
                            <p><strong>Emri Tregtar:</strong> {{ invoice.issuer.tradeName  }}</p>
                            <p><strong>Lloji i Biznesit:</strong> {{ invoice.issuer.businessType  }}</p>
                            <p><strong>Numri Unik:</strong> {{ invoice.issuer.uniqueIdentifierNumber  }}</p>
                            <p><strong>Numri i TVSH-së:</strong> {{ invoice.issuer.vatNumber  }}</p>
                            <p><strong>Komuna:</strong> {{ invoice.issuer.municipality  }}</p>
                            <p><strong>Adresa:</strong> {{ invoice.issuer.address  }}</p>
                            <p><strong>Telefoni:</strong> {{ invoice.issuer.phone  }}</p>
                            <p><strong>Email:</strong> {{ invoice.issuer.email  }}</p>
                        </div>
                    </td>
                    <td class="business-card">
                        <h3>Fatura Për: {{ invoice.receiver.businessName }}</h3>
                        <div class="business-details">
                            <p><strong>Emri Tregtar:</strong> {{ invoice.receiver.tradeName  }}</p>
                            <p><strong>Lloji i Biznesit:</strong> {{ invoice.receiver.businessType  }}</p>
                            <p><strong>Numri Unik:</strong> {{ invoice.receiver.uniqueIdentifierNumber  }}</p>
                            <p><strong>Numri i TVSH-së:</strong> {{ invoice.receiver.vatNumber  }}</p>
                            <p><strong>Komuna:</strong> {{ invoice.receiver.municipality  }}</p>
                            <p><strong>Adresa:</strong> {{ invoice.receiver.address  }}</p>
                            <p><strong>Telefoni:</strong> {{ invoice.receiver.phone  }}</p>
                            <p><strong>Email:</strong> {{ invoice.receiver.email  }}</p>
                        </div>
                    </td>
                </tr>
            </table>
        </div>
        
        <!-- Invoice Items -->
        <div class="items-section">
            {% if invoice.items and invoice.items|length > 0 %}
            <div class="items-table-container">
                <table class="items-table">
                    <thead>
                        <tr>
                            <th style="width: 5%;">#</th>
                            <th style="width: 30%;">Përshkrimi</th>
                            <th style="width: 8%;" class="text-center">Sasia</th>
                            <th style="width: 12%;" class="text-right">Çmimi për Njësi</th>
                            <th style="width: 8%;" class="text-center">Vlera e TVSH</th>
                            <th style="width: 12%;" class="text-right">Vlera pa TVSH</th>
                            <th style="width: 10%;" class="text-right">Vlera e TVSH</th>
                            <th style="width: 13%;" class="text-right">Totali</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% set items = invoice.items|sort((a, b) => a.sortOrder <=> b.sortOrder) %}
                        {% for item in items %}
                        <tr>
                            <td class="text-center">{{ loop.index }}</td>
                            <td class="description">{{ item.description }}</td>
                            <td class="text-center">{{ item.quantity|number_format(2, '.', '') }}</td>
                            <td class="text-right">{{ item.unitPrice|number_format(2, '.', '') }} €</td>
                            <td class="text-center">
                                {% if item.tax %}
                                    {% if item.tax.rate is null %}
                                        <span class="tax-exempt">E përjashtuar</span>
                                    {% else %}
                                        {{ item.tax.rate }}%
                                    {% endif %}
                                {% else %}
                                    <span class="tax-exempt">E përjashtuar</span>
                                {% endif %}
                            </td>
                            <td class="text-right">{{ item.subtotal|number_format(2, '.', '') }} €</td>
                            <td class="text-right">{{ item.taxAmount|number_format(2, '.', '') }} €</td>
                            <td class="text-right"><strong>{{ item.total|number_format(2, '.', '') }} €</strong></td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% else %}
            <p style="text-align: center; padding: 20px; color: #666;">Nuk ka artikuj në këtë faturë.</p>
            {% endif %}
        </div>
        
        <!-- Totals and Bank Accounts -->
        <div class="totals-section">
            <table class="totals-wrapper">
                <tr>
                    {% if invoice.issuer.bankAccounts and invoice.issuer.bankAccounts|length > 0 %}
                    <td class="bank-accounts-wrapper">
                        <div class="bank-accounts-card">
                            <h4>Llogaritë Bankare:</h4>
                            {% for bankAccount in invoice.issuer.bankAccounts %}
                            <div class="bank-account-item">
                                {% if bankAccount.bankName %}
                                <p>Banka: {{ bankAccount.bankName }}</p>
                                {% endif %}
                                {% if bankAccount.iban %}
                                <p>IBAN: {{ bankAccount.iban }}</p>
                                {% endif %}
                                {% if bankAccount.swift %}
                                <p>SWIFT: {{ bankAccount.swift }}</p>
                                {% endif %}
                                {% if bankAccount.bankAccountNumber %}
                                <p>Numri i Llogarisë: <strong>{{ bankAccount.bankAccountNumber }}</strong></p>
                                {% endif %}
                            </div>
                            {% if not loop.last %}
                            <div style="border-bottom: 1px solid #e0e0e0; margin: 8px 0;"></div>
                            {% endif %}
                            {% endfor %}
                        </div>
                    </td>
                    {% endif %}
                                        <td>
                        <div class="totals-card">
                            <table class="totals-table">
                                <tr>
                                    <td class="total-label">Vlera pa TVSH:</td>
                                    <td class="total-value">{{ invoice.subtotal|number_format(2, '.', '') }} €</td>
                                </tr>
                                <tr class="total-tax">
                                    <td class="total-label" colspan="2">
                                        TVSH dhe perqindja:
                                        {% if taxGroups|length > 0 %}
                                        <table class="tax-subtable">
                                            {% for key, group in taxGroups %}
                                                {% if group.total|number_format(2, '.', '')|number_format(2, '.', '') > 0 %}
                                                <tr>
                                                    <td class="tax-rate-label">{{ group.label }}</td>
                                                    <td class="tax-rate-value">{{ group.total|number_format(2, '.', '') }} €</td>
                                                </tr>
                                                {% endif %}
                                            {% endfor %}
                                            <tr style="border-top: 1px solid #e0e0e0; margin-top: 4px; padding-top: 4px;">
                                                <td class="tax-rate-label" style="font-weight: 600;">Totali TVSH:</td>
                                                <td class="tax-rate-value" style="font-weight: 600;">
                                                    {% set taxTotal = invoice.total - invoice.subtotal %}
                                                    {{ taxTotal|number_format(2, '.', '') }} €
                                                </td>
                                            </tr>
                                        </table>
                                        {% else %}
                                        <div style="text-align: right; margin-top: 8px;">
                                            {% set taxTotal = invoice.total - invoice.subtotal %}
                                            {{ taxTotal|number_format(2, '.', '') }} €
                                        </div>
                                        {% endif %}
                                    </td>
                                </tr>
                                <tr class="total-final">
                                    <td class="total-label">Totali:</td>
                                    <td class="total-value">{{ invoice.total|number_format(2, '.', '') }} €</td>
                                </tr>
                            </table>
                        </div>
                    </td>
                </tr>
            </table>
        </div>
        
        <!-- Signature Section -->
        <div class="signature-section">
            <table class="signature-table">
                <tr>
                    <td class="signature-cell">
                        <div class="signature-line">
                            {{ invoice.issuer.businessName }}
                        </div>
                    </td>
                    <td class="signature-cell">
                        <div class="signature-line">
                            {{ invoice.receiver.businessName }}
                        </div>
                    </td>
                </tr>
            </table>
        </div>
        
        <!-- Footer -->
        <div class="invoice-footer">
            Kjo faturë i nënshtrohet LIGJI NR. 03/L-008 Për procedurën përmbarimore. Te paguhet maksimumi 5 ditë nga dita e pranimit.
        </div>
    </div>
</body>
</html>