###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=your-secret-key-change-this-in-production
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
POSTGRES_DB=invoicing
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# DATABASE_URL will be constructed automatically from above variables
# Or set it explicitly if needed:
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/invoicing?serverVersion=18.1&charset=utf8"
###< doctrine/doctrine-bundle ###

###> symfony/router ###
DEFAULT_URI=http://localhost:8000
###< symfony/router ###

###> lexik/jwt-authentication-bundle ###
# Generate a secure passphrase with: openssl rand -base64 32
# REQUIRED: Must be set for JWT token generation
JWT_PASSPHRASE=your-jwt-passphrase-change-this-in-production
###< lexik/jwt-authentication-bundle ###

###> symfony/mailer ###
# REQUIRED: Mailer DSN configuration
# For development, use null transport (emails are not sent, just logged)
# MAILER_DSN=null://null

# For production, configure with your email service (e.g., SMTP, SendGrid, etc.)
# Example SMTP: MAILER_DSN=smtp://user:pass@smtp.example.com:587
# Example SendGrid: MAILER_DSN=sendgrid://KEY@default
MAILER_DSN=null://null

# REQUIRED: Email sender configuration for verification emails
MAILER_FROM_EMAIL=noreply@fatureime.com
MAILER_FROM_NAME=Faturëime
###< symfony/mailer ###

###> frontend configuration ###
# REQUIRED: Frontend URL for email verification links
FRONTEND_URL=http://localhost:80
###< frontend configuration ###

###> cors configuration ###
# REQUIRED: Allowed origin for CORS requests (regex pattern supported)
# For development (supports localhost with any port):
CORS_ALLOW_ORIGIN=^https?://(localhost|127.0.0.1)(:[0-9]+)?$
# For production (specific domain):
# CORS_ALLOW_ORIGIN=https://yourdomain.com
###< cors configuration ###
